import { Meta } from "@storybook/react";

import { RegisterPage } from "./RegisterPage";

const meta = {
  title: "Rese/RegisterPage",
  component: RegisterPage,
  tags: ["autodocs"]
} satisfies Meta<typeof RegisterPage>;
export default meta;

export const Default = {};
